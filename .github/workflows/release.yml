name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  windows-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/desktop/package-lock.json
      - run: npm ci
        working-directory: apps/desktop
      - run: cargo test --workspace
      - run: npm run tauri build
        working-directory: apps/desktop
      - run: .\scripts\package-windows.ps1
      - name: Generate SHA-256 checksums
        shell: pwsh
        run: |
          Get-ChildItem dist/lan-code-*-windows-x64.zip,target/release/bundle/msi/*.msi,target/release/bundle/nsis/*.exe | ForEach-Object {
            $hash = (Get-FileHash -Algorithm SHA256 -LiteralPath $_.FullName).Hash.ToLowerInvariant()
            "$hash  $($_.Name)" | Set-Content -Encoding ascii -LiteralPath "$($_.FullName).sha256"
          }
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/lan-code-*-windows-x64.zip
            dist/lan-code-*-windows-x64.zip.sha256
            target/release/bundle/msi/*.msi
            target/release/bundle/msi/*.msi.sha256
            target/release/bundle/nsis/*.exe
            target/release/bundle/nsis/*.exe.sha256
